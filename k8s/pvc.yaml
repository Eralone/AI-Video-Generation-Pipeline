apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vkbot-pvc
  namespace: vkbot
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs

